GET all bookmarks
---
tags:
  - Bookmarks

parameters:
  - in: header
    name: Authorization
    required: true

responses:
  200:
    description: get all bookmarks  

  401:
    description: Fails to get items due to authentication error